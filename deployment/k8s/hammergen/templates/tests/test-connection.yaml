apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hammergen.fullname" . }}-test-connection"
  labels:
    {{- include "hammergen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hammergen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
